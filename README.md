# Stream Bot

Telegram stream views/reactions service bot.

## Setup

```bash
cp config.example.py config.py
# edit config.py with BOT_TOKEN, API_ID, API_HASH, ADMIN_IDS
./start
```

Runtime files such as `config.py`, Telegram session files, logs, and data JSON are intentionally ignored.

